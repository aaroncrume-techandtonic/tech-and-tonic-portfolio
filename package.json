{
  "name": "migratebea",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "sync:vocabulary": "node scripts/sync-vocabulary.mjs",
    "build": "npm run sync:vocabulary && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^8.0.1"
  }
}
